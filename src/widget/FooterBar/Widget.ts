import BaseWidget = require("core/BaseWidget.class");
export = FooterBar;
class FooterBar extends BaseWidget{
      constructor(params){
            super(params);
      }
      start(){
          console.log('test-2');
      }
}